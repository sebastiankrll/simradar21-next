// Create global instance/variable of ws and share it between the different components.
// Use api to fetch flight/airport/etc data. Populate the panels with inital data from api on server.
// Whenever the ws sends new data, fetch the data from the same api client-side.

// Use dexie.js for indexedDb. Also use api for this. Send current version and if old version send indexeddb data.

// Check if intervals are really cleared for sun and flights interpolation